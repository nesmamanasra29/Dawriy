import { useDocumentTitle } from '../../../shared/hooks/useDocumentTitle';

export function AdminDashboardPage() {
  useDocumentTitle('Dawriy | Admin Dashboard');

  return (
    <section>
      <h1>Admin Dashboard</h1>
      <p>Admin flow is ready for future features.</p>
    </section>
  );
}