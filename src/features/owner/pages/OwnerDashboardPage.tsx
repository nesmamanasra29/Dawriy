import { useDocumentTitle } from '../../../shared/hooks/useDocumentTitle';

export function OwnerDashboardPage() {
  useDocumentTitle('Dawriy | Owner Dashboard');

  return (
    <section>
      <h1>Owner Dashboard</h1>
      <p>Owner flow is ready for future features.</p>
    </section>
  );
}